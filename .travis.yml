sudo: required

services:
- docker

language: java
dist: trusty

jdk:
- openjdk8

cache:
directories:
- $HOME/.m2

script:
- mvn clean package -U
- docker build -t test-storitev .

after_success:
- docker login -u="oblacki" -p="oblacki123"
- docker push test-storitev